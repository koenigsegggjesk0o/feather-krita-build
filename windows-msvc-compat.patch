--- a/libs/resources/KisDatabaseTransactionLock.h
+++ b/libs/resources/KisDatabaseTransactionLock.h
@@ -23,6 +23,11 @@
     void lock();
     void unlock();
     void commit();
+
+    // Feather-Krita Windows CI build provisioning (NOT upstream code):
+    // MSVC STL eagerly compiles std::unique_lock<Adapter>::try_lock through
+    // the using-declaration in KisAdaptedLock. Provide a safe no-op.
+    bool try_lock() { return false; }
 
 private:
     QSqlDatabase m_database;

--- a/libs/flake/KoShapeBulkActionLock.h
+++ b/libs/flake/KoShapeBulkActionLock.h
@@ -24,6 +24,11 @@
     KoShapeBulkActionLockAdapter(const QList<KoShape*> &shapes);
     void lock();
     void unlock();
+
+    // Feather-Krita Windows CI build provisioning (NOT upstream code):
+    // MSVC STL requires try_lock on adapters passed to std::unique_lock;
+    // safe no-op (bulk actions are never try-locked at runtime here).
+    bool try_lock() { return false; }
 
     UpdatesList takeFinalUpdatesList();
 

--- a/libs/image/KisBlockBackgroundFrameGenerationLock.h
+++ b/libs/image/KisBlockBackgroundFrameGenerationLock.h
@@ -28,6 +28,10 @@
     void lock();
     void unlock();
 
+    // Feather-Krita Windows CI build provisioning (NOT upstream code):
+    // see KisDatabaseTransactionLock.h shim note. Safe no-op for MSVC.
+    bool try_lock() { return false; }
+
 private:
     KisImageAnimationInterface *m_interface;
 };
--- a/libs/global/KisScopedPerformanceLogger.h
+++ b/libs/global/KisScopedPerformanceLogger.h
@@ -10,7 +10,7 @@
 #include "kis_debug.h"
 #include "kritaglobal_export.h"
 
-class KRITAGLOBAL_EXPORT KisScopedPerformanceLogger
+class KisScopedPerformanceLogger
 {
     Q_DISABLE_COPY_MOVE(KisScopedPerformanceLogger)
 public:
--- a/libs/widgets/KoZoomActionState.cpp
+++ b/libs/widgets/KoZoomActionState.cpp
@@ -135,7 +135,9 @@
     auto it = std::lower_bound(standardLevels.begin(), standardLevels.end(),
                                zoom, fuzzyCompareLess);
 
-    return qMin(standardLevels.size() - 1, std::distance(standardLevels.begin(), it));
+    // Feather-Krita Windows CI: MSVC/clang-cl deduces int vs long long for qMin;
+    // narrow explicitly. (Build-provisioning shim, not upstream code.)
+    return qMin(standardLevels.size() - 1, static_cast<int>(std::distance(standardLevels.begin(), it)));
 }
 
 int KoZoomActionState::calcNearestStandardLevel() const
--- a/libs/image/commands_new/KisChangeCloneLayersCommand.h
+++ b/libs/image/commands_new/KisChangeCloneLayersCommand.h
@@ -24,6 +24,10 @@
 private:
     struct Private;
     QScopedPointer<Private> d;
+
+    // Feather-Krita Windows CI: out-of-line dtor so the pimpl stays
+    // incomplete at consumer sites (MSVC requires sizeof at new-expr).
+    ~KisChangeCloneLayersCommand() override;
 };
 
 #endif // KIS_CHANGE_CLONE_LAYERS_COMMAND_H_

--- a/libs/image/commands_new/KisChangeCloneLayersCommand.cpp
+++ b/libs/image/commands_new/KisChangeCloneLayersCommand.cpp
@@ -26,6 +26,7 @@
     }
     d->newSource = newSource;
 }
+KisChangeCloneLayersCommand::~KisChangeCloneLayersCommand() = default;
 
 void KisChangeCloneLayersCommand::redo()
 {
